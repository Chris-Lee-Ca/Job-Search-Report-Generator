[pytest]
testpaths = . tests job_providers providers
addopts = --ignore=test_scrape.py
markers =
    integration: marks tests that require external services, a browser, or test full pipeline flow (deselect with -m "not integration")
